afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release
                groupId = 'com.github.samanzamani.persiandate'
                artifactId = 'stable'
                version = '1.3.1'
            }
        }
    }
}